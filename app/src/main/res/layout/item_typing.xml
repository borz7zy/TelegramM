<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/typing_bar"
    android:layout_width="match_parent"
    android:layout_height="28dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    android:background="@color/surfaceColor">

    <ImageView
        android:id="@+id/ic_typing"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="8dp"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_typing_list"
        android:scaleType="centerInside"
        android:contentDescription="Icon status writing message"
        app:tint="@color/secondaryContainerColor"
        app:tintMode="src_in"/>

    <TextView
        android:id="@+id/typing_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:insetTop="0dp"
        android:insetBottom="0dp"
        android:insetLeft="0dp"
        android:insetRight="0dp"
        android:minWidth="0dp"
        android:textColor="@color/secondaryContainerColor"
        android:minHeight="0dp"
        android:text=""
        tools:text="Юрий печатает"/>

</LinearLayout>
